Vue 2.x 缺点
无法监听初始化时不存在的属性
数据是通过劫持原生的数组方法实现的， 所以 arr[i] = xxx 这种方式，不会触发响应式

computed 和 watch 的区别
computed 是计算属性， 会进行缓存，只有当依赖的值更新的时候才会变化
watch 是监听对应的属性变化，需要进行对应的操作

v-for 中 key 的作用

Proxy
function handler() {
  geg() {

  },
  set() {

  },
  deleteProxy() {

  }
}
Proxy(obj, handler)

